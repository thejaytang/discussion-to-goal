---
name: discussion-to-goal
description: Turn an existing discussion into a verifiable plan and Goal prompt, review it through relevant professional perspectives, and set or reuse a Goal and start work when explicitly requested. Use for discussion-to-goal handoffs; ordinary summaries, draft-only requests, and designing this skill do not start goals.
---

# 1. 用途与触发

使用用户当前对话的语言输出；支持英文和中文调用。保留代码、路径、工具名和状态字段的原文。

把用户已经讨论的真实意图转化为能够执行、验证和持续推进的目标。交付链条是：讨论归纳 → 专业审查 → 近期计划 → 完整目标提示词 → 设置 Goal → 执行第一步并继续推进。

- 用户要求“整理成目标并推进”“设置 Goal”，或直接调用 `$discussion-to-goal` 且没有限制为草稿时，执行完整流程。这一调用代表对本流程中设置 Goal 和启动范围内工作的明确请求，不再例行询问是否开始。
- 用户要求“只总结”“先给提示词”“不要执行”时，仅生成对应成果；普通讨论和自动匹配到本 Skill 本身不构成创建 Goal 的授权。设计、安装或解释本 Skill 也不等于调用它处理业务目标。
- 专业角色是分析视角，不是身份宣称或自动召集多个 Agent 的指令。根据任务选取有用视角，不演出虚构专家会诊，不以“世界顶级专家”等头衔替代验收标准。

# 2. 从讨论提炼目标

优先使用当前会话中相关的用户要求、接受的决策和最新修正。涉及已有项目时，按适用入口指令检查实际项目和当前证据。其他会话仅在用户指向或当前任务确需时读取；不要为补全上下文扫描全部历史或猜测缺失讨论。

提炼以下内容，简单任务可以合并成一小段：

- **预期结果与价值**：谁需要什么结果，结果解决哪个实际问题。
- **范围与交付物**：具体材料、工作对象、输出位置，以及用户明确排除的工作。
- **有效决策与约束**：已选方向、优先级、资源、期限、权限、保留要求和不能改变的条件。
- **当前起点**：哪些已经确认，哪些只是设想、假设、待验证或已被否决的方案。
- **关键缺口**：哪些未知会实质改变目标、验收、成本或授权。

助手曾提出但用户未接受的建议不能写成用户决策。最新修正覆盖被明确替代的旧决定，但不能静默丢失仍有效的原始需求。

对多轮、复杂讨论，形成精简的决策记录：`CONFIRMED` 为用户已确认约束，`PROVISIONAL` 为允许可逆试行的临时决策，`PROPOSED` 为尚未接受的建议，`OPEN` 为确需解决的缺口。保留每项关键决定的会话依据或项目来源。已否决方向退出当前计划；只有出现改变判断的新证据才重新考虑。

能够查证的缺口先查证；低影响缺口采用显式工作假设继续。只有会实质改变结果或授权的缺口才提最少量问题，同时推进不依赖答案的准备。如果当前没有可以辨认的讨论主题或工作对象，只询问要转化的讨论，不自行挑选历史项目。

# 3. 用专业视角改善目标

按任务需要采用下列视角；把发现落实到目标、约束、证据或下一步，不要求每次输出整张角色表。

| 视角 | 必须解决的问题 | 应形成的改进 |
|---|---|---|
| 需求分析师 / 产品负责人 | 交付物是否真正满足用户目的？哪些要求互相冲突？ | 结果描述、范围、优先级和明确取舍 |
| 领域专家 / 方案负责人 | 方案依赖哪些事实、方法、接口或前提？ | 适合该领域的实施方式和优先验证的假设 |
| 验收负责人 / 证据审查者 | 什么观察能证明完成？哪些证据会否定成功？ | 每项关键结果的验收方法、成功标准和证据载体 |
| 交付负责人 / 风险审查者 | 哪个依赖阻止推进？怎样以低成本验证并恢复失败？ | 首个行动、近期里程碑、检查点、备选路径和权限边界 |

对最影响结果的假设，再从反方审查者视角提出一个有区分力的反例或失败条件，转成验证动作。例如：评测器是否漏检、真实材料是否区别于演示、保存是否真的能重新载入。需要独立标签、留出样本或人工确认时明确安排，不能用多个角色的一致意见替代独立证据。

优先使用可观察的行为、真实产物和适当的定量指标。没有依据时不编造基线、预算、日期或质量阈值；必要的建议值注明为建议并说明依据。验收标准缺失时先测基线或澄清关键期望，不把空白填成任意百分比。

将“完美”“所有情况都通过”“一直优化”等无界要求转化为具体范围和可验证的阶段结果。阶段结果不能冒充整个愿景已完成；不能为方便宣布成功而降低用户已经确定的标准。领域之间切换时选择相应专家视角，例如研究方法、统计、编辑或用户体验，不把所有任务套成软件工程。

# 4. 形成计划与可直接使用的目标提示词

采用滚动规划：详细说明当前阶段和下一次有区分力的验证，后续阶段只保留方向与依赖。每个关键检查点明确待验证问题、需要的证据、成功条件及失败后的调整方式。

复杂目标建立轻量的对应关系：`用户需求 → 可观察结果 → 行动/依赖 → 验收证据`。查找没有行动覆盖的需求、没有需求依据的行动、互相矛盾的完成标准，以及未经测量的质量承诺。仅在有助于跨阶段追踪时分配稳定编号；简单任务用短清单即可。

从结果倒推验证：结果成立需要哪些真实事实、哪些产物和连接，以及什么现场证据能证明整条用户路径有效。区分“产物存在”“有实质内容”“已接入正常路径”“真实输入产生正确结果”。在研究、文档等任务中对应检查主张、分析/成稿、来源关联与实际核验，不机械要求软件接口。

总目标、当前阶段和下一检查点分开表达。各必需维度单独判定：功能、质量、实际使用、外部/人工验收不能互相抵消。质量指标适用时写明样本范围、分母、基线、目标/留出样本及关键失败，缺证据标为 `UNMEASURED`，存在失败标为 `FAIL`，外部依赖阻碍标为 `BLOCKED`；这些是验收项标签，不自动触发 Goal 状态更新。

若用户给出时间窗口，写明窗口内优先产出、验证与交接安排；到时保留未完成项，不把时间用满等同目标完成。后续路径可以回到研究或设计阶段，但不据此重开已解决的问题或重写已确认的验收标准。

写出自包含的目标提示词，使执行者不必依赖“如上讨论”才能开工。按复杂度合并以下内容，删除无实际内容的栏目，不残留占位符：

```text
目标：说明受益者、要实现的结果、任务范围与具体交付物。

依据与起点：列明适用项目、必要材料和已确认现状；区分有效决定、工作假设及待验证事项。

执行视角：说明本任务需要的专业判断及各自应落实的检查，不堆叠专家头衔。

完成标准：逐项列出关键结果、验收方法及证据；保留用户已确定的阈值和必须满足的条件。区分总目标、当前阶段和下一检查点，保留需求与行动、证据的对应关系。

约束与授权：保留明确的范围、优先级、资源、期限和权限；说明哪些后续操作已有授权，哪些必须取得新的明确授权。

推进方式：先执行具体的第一步，再根据证据滚动调整近期计划。验证失败时先诊断，再选择有依据的修复或替代路径。分别记录已取得的进展、经核实仍在运行的等待、无新证据的停滞，避免无信息增益的重复重试。

进展与交付：持续推进已授权范围内的工作，汇报关键发现。逐项核对完成条件并提交证据、结果位置和剩余缺口；不得以计划、演示或局部测试替代整体完成。
```

生成后做一次一致性检查：关键用户要求是否全部覆盖，计划是否能产生所需证据，首步是否可执行，是否混淆愿望与事实，是否暗增工作范围或权限。只展示影响决策的修正理由，不输出冗长的内部推理。

默认将最终提示词完整呈现在用户可见、可复制的消息正文中，并简短说明首步。遵守当前环境的输出格式要求，不只给文件路径或摘要。展示不是审批检查点，已有启动授权时在同一轮继续设置 Goal 和执行。

有持续维护的项目时优先更新已有目标/计划材料，按项目约定维护状态摘要；需要新材料时放入对应 `project-support/`。简单任务不创建整套项目文档。

# 5. 设置 Goal 并开始执行

使用当前环境实际提供的 Goal 工具，调用前以实时工具定义为准。不要伪造 API、直接修改应用数据库或用计划文件冒充 Goal 状态。

1. 调用 `get_goal` 检查当前目标。若工具不可用，保留已生成提示词，准确说明尚未设置 Goal；对于已经获得执行授权的工作，继续普通执行，不停在工具缺口上。
2. 没有未完成目标且已获本流程的启动授权时，调用 `create_goal`，将完整最终提示词作为 `objective`。除非用户明确请求 token 预算，否则省略 `token_budget`；时间期限不换算成自行设定的 token 数。
3. 如果当前已有语义相同的未完成目标，沿用并继续，不重复创建。若新要求实质改变目标内容或与现有目标不同，不能静默声称目标已更新。保留新提示词并说明冲突；继续已经授权且不依赖 Goal 切换的安全准备，包括独立的新任务准备，并明确这些工作尚未挂接新 Goal；需要切换时请用户在应用中处理现有 Goal。不得假标完成或假标阻塞来清空目标槽位，也不得擅自创建另一个任务绕开冲突。
4. 创建调用返回不确定结果时，先重新读取 Goal 状态再决定是否重试。读取状态确认 `objective` 与预期一致后，才报告“已设置 Goal”。若设置失败，报告具体状态，不把普通执行称作 Goal 已启动。
5. 设置成功后立即执行计划中的第一项实质行动，产生现场检查、基线、修改或其他任务相关证据。读 Goal 状态本身不是业务推进。不要以“目标已设置，需要时我可以继续”结束；持续完成当前已授权工作。

Goal 的完成和阻塞规则使用当前工具定义。当前接口中，`update_goal` 仅能标记 `complete` 或 `blocked`，不能用它编辑目标、暂停、恢复或调整预算。只有全部必需完成条件已验证、无剩余必要工作时才标记 `complete`。有显式 token 预算的 Goal 完成时，报告工具返回的最终使用量。

当前 `blocked` 条件要求同一阻碍连续至少三个 Goal 轮次出现，且没有用户输入或外部状态变化就无法再有实质进展；同一轮重试三次不算三个轮次。恢复过的阻塞目标按新一轮连续阻碍重新计数。需要用户回答时可以报告阻碍并等待，但不能为了达到计数而制造空转轮次。用户请求停止时遵守停止要求，不用虚假 `blocked` 或 `complete` 替代暂停。

# 6. 推进中的边界

- Goal 启动授权继承讨论中已经明确的操作授权，但不自动授权新的外发、发布、购买、账户变更或重要删除。需要额外批准时，先准备具体可审查的成果和验证证据再请求批准。
- 目标提示词和 Skill 本身不能保证后台常驻、定时唤醒或无限运行。只有用户明确要求后续提醒、定时或持续监控时，才按可用自动化工具处理；不要为了启动 Goal 自动创建定时任务。
- 未完成但仍可推进时继续工作；平台中断时保留简短、可恢复的状态。成功标准保持稳定，执行路径可随新证据调整。扩展或改变成功标准应有用户指令或明确接受的决定作依据。
- 每个关键检查点核对是否改变了实际产物、得到了会影响下一动作的新证据，或验证了具体任务句柄仍在运行。重复状态、重新写计划或复跑无关绿测不是新进展。无进展时换成能区分原因的最小验证；已证实无效的路径不重试，除非前提改变。轮询超时本身不证明任务已停止，应检查同一句柄而非重复启动。
- 恢复记录只保留当前阶段、已有证据、失败路径、确实仍在运行的句柄、未解决依赖和下一步；尽量更新已有项目记录，不增建平行状态体系。明确仍待真人或真实设备验证的项目，不能自行用本地模拟代替验收。
- 最终区分提示词已生成、Goal 已设置、已经执行的行动、验证过的完成条件及尚未满足的部分。只声称工具回执或任务证据能够证明的状态。

# 7. 维护依据

修改本 Skill 或需要解释设计取舍时，读取 [设计依据](references/design-rationale.md)。日常执行不必加载该文件。它记录通用设计经验与开源机制的取舍，不是额外的运行时依赖；不要自动安装或调用参考项目。
